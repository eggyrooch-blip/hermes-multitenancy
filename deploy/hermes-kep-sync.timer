# 每日触发 hermes-kep-sync.service（凌晨低峰，随机错峰）。
[Unit]
Description=Run Hermes kep-cli registry sync daily

[Timer]
OnCalendar=*-*-* 04:20:00
RandomizedDelaySec=20min
Persistent=true

[Install]
WantedBy=timers.target
